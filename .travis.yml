language: java
script: mvn clean verify

sudo: required

services:
  - docker

deploy:
  provider: gae
  project: restaurant-solidaire-backend
  keyfile: client-secret.json
  verbosity: debug
  on: master
